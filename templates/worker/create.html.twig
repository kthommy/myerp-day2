{% extends 'base.html.twig' %}

{% block title %}
{{ 'worker.create.title' | trans }} - {{ parent() }}
{% endblock %}

{% block body %}
{{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="{{ 'worker.action.create' | trans }}" />
{{ form_end(form) }}
{% endblock %}

{% block stylesheets %}
{{ parent() }}
<link rel="stylesheet" href="{{ asset('css/worker.css') }}" />
{% endblock %}
